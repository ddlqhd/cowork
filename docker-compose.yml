version: '3.8'

services:
  websocket-sse-server:
    build: .
    ports:
      - "8080:8080"
    environment:
      - HOST=0.0.0.0
      - PORT=8080
      - LOG_LEVEL=INFO
      - DEBUG=false
    restart: unless-stopped
