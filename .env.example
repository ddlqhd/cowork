# Server
HOST=0.0.0.0
PORT=8080
DEBUG=false

# WebSocket
WS_PATH=/ws
WS_PING_INTERVAL=30
WS_PING_TIMEOUT=10

# SSE
SSE_PATH=/sse/push
SSE_BATCH_PATH=/sse/push/batch

# Logging
LOG_LEVEL=INFO
LOG_FORMAT={time:YYYY-MM-DD HH:mm:ss} | {level} | {message}

# Security
CORS_ORIGINS=*
